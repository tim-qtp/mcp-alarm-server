spring:
  ai:
    mcp:
      server:
        name: alarm-mcp-server
        version: 0.0.1
#        type: ASYNC
        # sse
        stdio: false
        sse-endpoint: /sse

  main:
    web-application-type: reactive
    banner-mode: off
